SPEEDTEST_TRACK_APP_KEY=base64:your_speedtest_app_key_here
PID=1000
GID=1000

# Allocated by ../homelab-infra/registry.yaml (services.speedtest.upstream.port) — do not change locally.
SPEEDTEST_HOST_PORT=20016

# Host port for Homepage (container listens on 3000)
# Allocated by ../homelab-infra/registry.yaml (services.homepage.upstream.port) — do not change locally.
HOMEPAGE_HOST_PORT=20017

# Comma-separated hosts (include ports if needed) allowed by Homepage
HOMEPAGE_ALLOWED_HOSTS=home.stanley.arpa,localhost,127.0.0.1
